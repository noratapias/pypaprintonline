// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.pypaprint.pedidos.web.security;

import com.pypaprint.pedidos.domain.security.AuthorityPrincipalAssignment;
import com.pypaprint.pedidos.services.AuthorityPrincipalAssignmentService;
import com.pypaprint.pedidos.services.AuthorityService;
import com.pypaprint.pedidos.services.UsuarioService;
import com.pypaprint.pedidos.web.security.RoleMappingController;
import java.io.UnsupportedEncodingException;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect RoleMappingController_Roo_Controller {
    
    @Autowired
    AuthorityPrincipalAssignmentService RoleMappingController.authorityPrincipalAssignmentService;
    
    @Autowired
    AuthorityService RoleMappingController.authorityService;
    
    @Autowired
    UsuarioService RoleMappingController.usuarioService;
    
    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String RoleMappingController.create(@Valid AuthorityPrincipalAssignment authorityPrincipalAssignment, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, authorityPrincipalAssignment);
            return "security/assignments/create";
        }
        uiModel.asMap().clear();
        authorityPrincipalAssignmentService.saveAuthorityPrincipalAssignment(authorityPrincipalAssignment);
        return "redirect:/security/assignments/" + encodeUrlPathSegment(authorityPrincipalAssignment.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String RoleMappingController.createForm(Model uiModel) {
        populateEditForm(uiModel, new AuthorityPrincipalAssignment());
        return "security/assignments/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String RoleMappingController.show(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("authorityprincipalassignment", authorityPrincipalAssignmentService.findAuthorityPrincipalAssignment(id));
        uiModel.addAttribute("itemId", id);
        return "security/assignments/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String RoleMappingController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, @RequestParam(value = "sortFieldName", required = false) String sortFieldName, @RequestParam(value = "sortOrder", required = false) String sortOrder, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("authorityprincipalassignments", AuthorityPrincipalAssignment.findAuthorityPrincipalAssignmentEntries(firstResult, sizeNo, sortFieldName, sortOrder));
            float nrOfPages = (float) authorityPrincipalAssignmentService.countAllAuthorityPrincipalAssignments() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("authorityprincipalassignments", AuthorityPrincipalAssignment.findAllAuthorityPrincipalAssignments(sortFieldName, sortOrder));
        }
        return "security/assignments/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String RoleMappingController.update(@Valid AuthorityPrincipalAssignment authorityPrincipalAssignment, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, authorityPrincipalAssignment);
            return "security/assignments/update";
        }
        uiModel.asMap().clear();
        authorityPrincipalAssignmentService.updateAuthorityPrincipalAssignment(authorityPrincipalAssignment);
        return "redirect:/security/assignments/" + encodeUrlPathSegment(authorityPrincipalAssignment.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String RoleMappingController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        populateEditForm(uiModel, authorityPrincipalAssignmentService.findAuthorityPrincipalAssignment(id));
        return "security/assignments/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String RoleMappingController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        AuthorityPrincipalAssignment authorityPrincipalAssignment = authorityPrincipalAssignmentService.findAuthorityPrincipalAssignment(id);
        authorityPrincipalAssignmentService.deleteAuthorityPrincipalAssignment(authorityPrincipalAssignment);
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/security/assignments";
    }
    
    void RoleMappingController.populateEditForm(Model uiModel, AuthorityPrincipalAssignment authorityPrincipalAssignment) {
        uiModel.addAttribute("authorityPrincipalAssignment", authorityPrincipalAssignment);
        uiModel.addAttribute("authoritys", authorityService.findAllAuthoritys());
        uiModel.addAttribute("usuarios", usuarioService.findAllUsuarios());
    }
    
    String RoleMappingController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
